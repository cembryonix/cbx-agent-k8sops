"""Global styles for the application."""

import reflex as rx

# Warm background colors matching Claude's aesthetic
WARM_BG_MAIN = "#FAF9F6"  # Warm cream for main content area
WARM_BG_SIDEBAR = "#F3F0E8"  # Warmer tan for sidebar

# Base styles applied to all pages
base_style = {
    "font_family": "system-ui, -apple-system, sans-serif",
    "background_color": WARM_BG_MAIN,
}

# Stylesheet URLs
stylesheets = [
    # Add any external stylesheets here
]
